<?xml version="1.0" encoding="UTF-8"?><!-- 命名空间:主要的作用是用来约束XML文件，并验证XML文件有效性。定义了可以使用的节点和属性。 -->
<!-- beans整个配置文件的根节点，包含一个或多个bean元素 -->
<!-- 前两个是最基本的命名空间定义、mvc、启用自动注解时的命名空间定义、启用事务管理时的命名空间定义、启用AOP模块时的命名空间定义 -->
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation=" 
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.1.xsd 
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd 
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd 
        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.1.xsd
        http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.1.xsd"
	default-lazy-init="true"><!-- 启用懒加载 ，减少系统开销，在调用时常见Bean -->
	<!-- 以上是与上述命名空间配套的shamal文件装载路径 -->
	<!-- 配置自动AOP -->
	<!-- <aop:aspectj-autoproxy /> -->

	<!-- 自动扫描包内的注解，默认扫描@controller,@Service,@Repository等注解 -->
	<context:component-scan base-package="com.kite">
		<!-- 添加黑名单，不扫描Controller注解 -->
		<context:exclude-filter type="annotation"
			expression="org.springframework.stereotype.Controller" />
	</context:component-scan>
	
	<!-- 配置sessionFactory -->
	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
		<property name="configLocation" value="classpath:hibernate.cfg.xml"></property>
	</bean>

	<!-- 配置 Spring的声明式事务 -->
	<bean id="transactionManager"
		class="org.springframework.orm.hibernate4.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory"></property>
	</bean>

	<!-- 事务注解驱动，可以指定cglib代理 -->
	<tx:annotation-driven
		transaction-manager="transactionManager" proxy-target-class="true" />

	<!-- <aop:config> <aop:pointcut expression="execution(* com.iflytek.cainiao.service.impl.*.*(..))" 
		id="myPointcut"/> <aop:advisor advice-ref="myadvice" pointcut-ref="myPointcut"/> 
		</aop:config> -->

	<!-- 配置事务增强 -->
	<!-- <tx:advice id="myadvice" transaction-manager="transactionManager"> 
		<tx:attributes> <tx:method name="insert*" propagation="REQUIRED"/> <tx:method 
		name="update*" propagation="REQUIRED"/> <tx:method name="delete*" propagation="REQUIRED"/> 
		<tx:method name="buy*" propagation="REQUIRED"/> </tx:attributes> </tx:advice> -->

</beans>

